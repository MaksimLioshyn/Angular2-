import { ChangeFontWeightDirective } from './change-font-weight.directive';

describe('ChangeFontWeightDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeFontWeightDirective();
    expect(directive).toBeTruthy();
  });
});
